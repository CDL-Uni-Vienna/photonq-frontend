import { css } from "@emotion/react";
import styled from "@emotion/styled";

export const BubbleStyle = styled.div((props) => {
  return css`
    .qubit {
      width: 200px;
      height: auto;
    }
  `;
});
